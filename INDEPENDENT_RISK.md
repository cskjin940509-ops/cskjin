# v5.1 独立风险执行

## 生产链路

- 专用交易任务保留原账本写入互斥组，不取消正在写账本的任务；开仓、止盈止损、换仓及T均由这一写入任务维护。
- 雷达/日线/完整成分/双源资金成为独立证据生产者，使用独立队列，只发布 astock_radar 和 astock_selection_evidence，不写账本、状态或组合估值。主雷达及后续完整证据各自在到达时通知交易任务。
- 交易引擎只读取已发布证据，获取当前主行情后直接检查；不调用慢历史、成分或独立资金接口。技术指标从缓存真实日线重算，过期成分排名不视为合格。缓存缺失沿用原状态证据，不能默认验证通过。
- 即使没有新雷达，交易时段内的硬止损、移动保护、已确认日终退出和组合风险仍检查；过期或缺失个股行情不模拟成交。雷达过期仍阻止新仓和T，不使用旧盘中资金/过热信号新增减仓指令。
- 同起点无T对照采用相同的独立退出通路。休市不假造成交。
- 独立行情批量与单股回退改为最多6路并发，避免40只个股串行超时阻塞。未返回的证据仍标记缺失。
- 同期基准和历史日收益重建移到收盘后，避免盘中占用账本队列；盘中资产、当日收益与持仓估值仍每次交易任务更新。日线曲线继续在收盘后更新。

## 防守降仓

DEFENSIVE 状态使用大盘 availableAt，而不是雷达 capturedAt。相同仓位上限的2个新鲜快照、间隔至少60秒确认后，立即按上限分配减仓量。快照重复不计数，超过15分钟间隔重置，恢复非防守状态重置。旧快照不触发新的防守确认。

该减仓走 PORTFOLIO_RISK，不受完整持有2天、普通调仓窗口、20%普通换手上限限制。指数急跌、当日净值及收盘回撤等既有紧急风险仍优先。不增加资金、不制造成交、不改历史；T+1、跌停和容量限制仍保留未成交数量。仓位超过大盘上限时禁止新增风险。

GitHub Actions 的启动/排队和行情服务仍不提供实时保证。本次消除的是慢证据对风险检查的程序依赖与共用队列，不能承诺零延迟或停机为零。参数需前向验证。
