# 按排名分批建仓 v5.3

用户确认的目标：市场允许进仓后，对股票排序并执行分批建仓；辅助指标不再要求全部同时通过。

## 决策和执行

- 沿用 v5.2 文档方向型情绪仓位映射。盘中只收紧盘前上限。
- 个股分数、板块排名、阶段、5/20量比和回踩形态作为排序偏好；每项偏好未满足扣3分。原始得分保留。
- 数据缺口保持空值及缺口说明。首次建仓有缺口或偏好未满足时，单股目标最多1%；不以低于64分全盘禁买。
- 新仓与已确认的部分成交余量，在有效交易时段且14:50之前按排名执行；首批需一个新鲜主行情，不再重复等待三个形态快照。
- 同一股票相同时间戳的行情不重复尝试买入。已持有股票的普通加仓仍需三次确认、原加仓窗口和加仓条件。
- 已确认的资金全面负向、明显追高/走弱、风险警示、停牌、主行情缺失或过期，以及仓位/流动性/涨跌停等风险条件继续约束执行。
- 卖出继续遵循止损、移动保护、分层止盈、日级失效、T+1及成交容量。没有重写历史交易或人工填充持仓。

## 应用字段

生产入口仍为 `tools/run_ai_dynamic_portfolio_v2_2.py`，读取同一策略引擎。
`latest.json` 的 `strategyVersion` 为 `v5.3-ranked-staged-entry`。
`selection45.entryPolicy` 为 `RANKED_STAGED_ENTRY`。
候选包含原始 `score`、`rankingScore`、`rankingWarnings`、缺口、目标仓位及执行状态。
现有 Android 组件读取 `decisionPlan` 和 `rulesZh`，可直接展示买入、部分成交、等待新行情、额度不足等状态。
没有支撑区间时，买入区间字段仅展示采集时参考价格；解释字段注明参考时间和实际成交需重验。
自动执行摘要从当前候选重算，不把缺少可选数据列为拒绝原因。

## 验证

`tools/test_ranked_entry_v53.py` 使用隔离的合成行情与内存账本验证真实模拟成交路径。
覆盖低分且缺历史时首次建仓、重复快照、零市场额度、失去流动性、过期行情、午休及明确负向资金。
现有止损、T+1、部分成交、组合限额和研究版本迁移测试继续运行。
生产验证须检查真实自动任务的完成状态和发布后的版本，不能以代码提交代替上线成功。
